# cloud-resume-analyzer

💻 Local Setup (Frontend)

If your project also has a React frontend (for uploading job descriptions or resumes), follow these steps to set it up locally.

🧩 Prerequisites

Make sure you have installed:

Node.js
 (v18+ recommended)

npm

AWS credentials configured (aws configure) if using local AWS SDK

🛠️ Installation Steps

1️⃣ Clone the repository

git clone https://github.com/vaidehipatel311/cloud-resume-analyzer.git


2️⃣ Navigate into the project

cd repo-name
cd frontend


3️⃣ Install dependencies

npm install


4️⃣ Start the development server

npm start


This will run the app on http://localhost:3000
 by default.


